"""LatheOS embedded shell.

Single-window Jarvis-style TUI: hardware HUD + chat + terminal + diff viewer.
Runs fully offline once the local LLM and Piper voices are on `/assets`.
"""

__version__ = "0.1.0"
